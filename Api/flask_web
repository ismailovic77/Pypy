from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello, World! This is a website running on port 80. this request is from the router ip"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=80)
